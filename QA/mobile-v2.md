# 手机界面改版核查记录

更新：2026-09-14。

## 变更与保留

`dist/index.html`、`style.css`、`app.js` 重新实现统一手机界面；`research.html` 保留为带原锚点的兼容入口。新增与dist完全相同的docs树，用作Pages的main/docs发布源。

原始 `data.json` blob 保持 `3a2fe9bdf8b9aa1de37b8168714a6c7ac7ec78ec`；研究正文保持 `cd5f2dbda103d519bd074ee6526119c2c7d34502`；图片目录保持 `9f93486151d185c8eefa4b2a57b756efc00596e6`。不把本地测试占位图或13条测试数据上传到这两个目录。

## 已完成的本地验证

JavaScript语法检查、Python脚本编译检查通过。使用Chromium内存页面测试320、375、390、430、768、1440像素宽度，检查页面无横向溢出、缩略图目录与搜索、前后选画、大图与返回位置、全文排版、10幅配图位置、30组引用、章节目录、四组对照、内嵌离线版无HTTP图片请求。

**本地测试使用13条标明用途的中性测试图和真实研究正文，不是全部58张作品图的视觉验收。** 本地环境无法通过浏览器导航到测试服务器，也无法正常下载私有仓库二进制文件。因此，真实资产解码、WebKit、真实浏览器历史导航和GitHub公开站点，不冒称已在本地检验。

发现并修正的布局问题：在限制高度的选画对话框中，CSS Grid可能压缩卡片行高使图片溢出。已固定自动行高为max-content并让整个目录成为唯一滚动区域；六种宽度均重测。

## 真实资产及跨引擎验证

新增 `scripts/package_mobile.py` 校验全部58件记录、图片文件解码、SHA-256、研究配图与CR链接、原有归一化坐标，并生成完整单文件离线版。

新增 `scripts/test_mobile.py` 在Chromium和WebKit、六种宽度下运行真实图像与手机操作检查，并测试旧研究链接、全部58张图片和离线版的零HTTP载图。GitHub Actions 工作流已加入；其实际成功状态以对应提交的Actions结果为准，不能以“添加测试代码”代替“测试已通过”。

## 发布边界

2026-09-14检查到仓库private=true、has_pages=false。没有改变可见性或绕过管理权限。docs目录已经具备独立静态发布所需的全部资产；仍须仓库拥有者在Settings → Pages中选择Deploy from a branch、main、/docs并保存。实际公网可访问性应在GitHub完成部署后再验证。
